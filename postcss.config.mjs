const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};
export default config;
module.exports = {
  theme: {
    extend: {
      fontFamily: {
        sans: ['Inter', 'sans-serif'], // Define Inter como fonte padrão (sans)
      },
    },
  },
  plugins: [],
}